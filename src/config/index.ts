import databaseConfig from './database.config'
import messagingConfig from './messaging.config'
import uploadConfig from './upload.config'

export default [databaseConfig, messagingConfig, uploadConfig]
